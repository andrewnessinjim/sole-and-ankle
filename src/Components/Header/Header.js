import MainHeader from "../MainHeader";
import SuperHeader from "../SuperHeader";

export default function Header() {
    return (
        <header>
            <SuperHeader/>
            <MainHeader/>
        </header>
    )
}